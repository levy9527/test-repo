branches:
  only:
    - master
language: node_js
node_js:
- lts/*
git:
  depth: 3
install:
- npm install -g @lhci/cli 
script:
- lhci autorun --upload.target=temporary-public-storage --collect.staticDistDir=./
addons:
  chrome: stable
#- ./build.sh
after_success:
#- ./notify.sh
cache: yarn
